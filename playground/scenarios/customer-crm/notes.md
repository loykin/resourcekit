Acceptance notes:

- Uses generic kind names, not kit-prefixed names.
- Selection state is a flat page variable.
- Table features are selected through spec flags rather than hand-placed search controls.
